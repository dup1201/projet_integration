<!DOCTYPE html>
<html>
<head>
</head>
<body>

<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
    <button type="button" class="navbar-toggler" data-toggle="collapse" data-target="#monmenu" aria-controls="navbarTogglerDemo02" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span></button>
    <a class="navbar-brand" href="{{ path ('accueil') }}">Shop</a>
    <div class="collapse navbar-collapse" id="monmenu">
        <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
            <li class="nav-item active"><a class="nav-link" href="{{ path ('accueil') }}"> Accueil </a></li>
            <li class="nav-item active"><a class="nav-link" href="{{ path('products') }}"> Produits </a></li>

            {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                <li class="nav-item active"><a class="nav-link" href="{{ path('fos_user_profile_show') }}">mon
                        compte</a>
                </li>
            {% endif %}

            {% if is_granted("ROLE_ADMIN") %}
                <li class="nav-item active"><a class="nav-link" href="{{ path ('users') }}">liste des utilisateurs</a>
                </li>
            {% endif %}
            {% if is_granted("ROLE_GESTIONNAIRE")or is_granted("ROLE_ADMIN")  %}
                <li class="nav-item active"><a class="nav-link" href="{{ path ('productsgestion') }}">Gestion des
                        produits</a></li>
                {% endif %}

        </ul>
        <ul class="navbar-nav navbar-right">
            {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                <li class="nav-item active"><a class="nav-link" href="{{ path('fos_user_security_logout') }}">
                        se déconnecter
                    </a></li>
            {% else %}
                <li class="nav-item active"><a class="nav-link" href="{{ path('fos_user_registration_register') }}">
                        S'inscrire </a></li>
                <li class="nav-item active"><a class="nav-link" href="{{ path('fos_user_security_login') }}">se
                        connecter</a></li>
            {% endif %}
        </ul>
    </div>
</nav>

</body>
</html>
