{% extends 'base.html.twig' %}

{% block body %}

    <div align="center">
        <h1>Adresse(s)</h1>
        <table class="list">
            <tr>
                <th>Numéro</th>
                <th>Boite</th>
                <th>Rue</th>
                <th>Code postal</th>
                <th>Ville</th>
                <th>Pays</th>
                <th>Action</th>
            </tr>
            {% for adresse in adresseList %}
            <tr>
                <td>{{ adresse.numero }}</td>
                <td>{{ adresse.boite }}</td>
                <td>{{ adresse.rue }}</td>
                <td>{{ adresse.codePostal }}</td>
                <td>{{ adresse.ville }}</td>
                <td>{{ adresse.pays }}</td>


                <td>&nbsp;
                    <a class="action" href="{{ path('editadresse', {"id":adresse.id}) }}">edit</a>&nbsp;
                    <a class="action" href="{{ path ('deleteadresse', {"id":adresse.id}) }}">delete</a></td>
            </tr>
            {% endfor %}
        </table>
        <br>
        <a class="add-button" href="{{ path('addadresse') }}">Ajouter une adresse</a>
    </div>


{% endblock %}