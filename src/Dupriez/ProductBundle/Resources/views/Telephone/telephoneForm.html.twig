{% extends 'base.html.twig' %}


{% block body %}

    <h1>Ajout ou modification d'un telephone</h1>

    {{ form_start(telephoneForm, {'attr': {'novalidate': 'novalidate'}}) }}

    {{ form_widget(telephoneForm) }}

    <button type="submit">Envoyer</button>

    {{ form_end(telephoneForm) }}

{% endblock %}