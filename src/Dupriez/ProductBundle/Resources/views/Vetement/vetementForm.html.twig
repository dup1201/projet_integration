{% extends 'base.html.twig' %}


{% block body %}

    <h1>Ajout ou modification d'unvetement</h1>

    {{ form_start(vetementForm, {'attr': {'novalidate': 'novalidate'}}) }}

    {{ form_widget(vetementForm) }}

    <button type="submit">Envoyer</button>

    {{ form_end(vetementForm) }}

{% endblock %}