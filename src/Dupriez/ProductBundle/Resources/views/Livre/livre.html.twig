{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">

        <h1>Livre</h1>

        <div class="row">
            {% for livre in livreList %}
                <div class="col-6">
                    <div class="card">
                        <div class="media">
                            <img alt="livre" src="{{ asset('uploads/images/' ~ livre.products.image) }}" style="height: 200px">
                            <div class="media-body container" >
                                <h5 class="card-title">
                                    {{ livre.products.prix }}€
                                </h5>
                                <a class="btn btn-primary" href="{{ path('addpanier', {'id' :livre.products.id}) }}">
                                    Ajouter au panier</a>
                            </div>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>

    </div>
{% endblock %}